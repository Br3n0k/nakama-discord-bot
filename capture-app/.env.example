# Variáveis de Ambiente para Nakama Capture App (Electron)

# URL do WebSocket do Backend Nakama
# O App de Captura se conectará a este endpoint para enviar áudio.
NAKAMA_BACKEND_WS_URL="ws://localhost:3000/api/audio/stream"

# (Opcional) Se houver outras configurações que precisam ser gerenciadas via .env
# Por exemplo, se o app precisar de uma API key específica para alguma integração.
# NAKAMA_CAPTURE_API_KEY="some_optional_api_key"

# Configurações de logging ou debug
# ELECTRON_LOG_LEVEL="info" # Pode ser usado por electron-log, por exemplo

# Nota: Muitas configurações do Electron app (como ID da sessão do usuário, token JWT)
# são geralmente gerenciadas internamente via electron-store ou obtidas
# através de um fluxo de login (ex: abrir uma janela do browser para o dashboard Nakama
# e receber o token de volta via IPC ou custom protocol handler).
# Este .env.example é mais para URLs de backend ou chaves globais do app.

---
// Página de login - geralmente um redirecionamento ou lógica de tratamento
// Para este placeholder, apenas informamos o usuário.
// Em uma implementação real, esta página pode iniciar o fluxo OAuth2
// ou ser o alvo do redirecionamento do backend após o `/auth/discord`.

// Astro.redirect('/api/auth/discord'); // Exemplo de como poderia ser
import Layout from '../layouts/Layout.astro';
---
<Layout title="Nakama - Login">
  <main class="container mx-auto px-4 py-16 text-center">
    <h1 class="text-3xl font-bold mb-6 text-purple-400">Login com Discord</h1>
    <p class="text-gray-300 mb-4">
      Você será redirecionado para o Discord para autenticação.
    </p>
    <p class="text-gray-400">
      Se o redirecionamento não ocorrer automaticamente, verifique se os pop-ups estão habilitados.
    </p>
    <!-- Em uma implementação real, esta página pode não ser visível ou apenas por um instante -->
  </main>
</Layout>

<script>
  // Idealmente, o backend lidaria com o início do fluxo OAuth.
  // Se o frontend precisar iniciar, seria algo como:
  // window.location.href = '/api/auth/discord'; // ou a URL direta do Discord OAuth
  // Mas, como especificado, o backend tem POST /auth/discord,
  // então o LoginButton na index.astro faria uma requisição para lá,
  // e o backend redirecionaria para o Discord.
</script>
